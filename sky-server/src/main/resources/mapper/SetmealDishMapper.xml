<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <insert id="insertBatch">
        insert into setmeal_dish
        (setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="setmealDishes" item="sd" separator=",">
            (#{sd.setmealId}, #{sd.dishId}, #{sd.name}, #{sd.price}, #{sd.copies})
        </foreach>
    </insert>
    <select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="ids" item="dishId" separator="," open="(" close=")">
            #{dishId}
        </foreach>
    </select>
    <select id="query" resultType="com.sky.vo.DishItemVO">
        SELECT
            d.name AS name,                     -- 菜品名称
            sd.copies AS copies,                -- 份数
            d.image AS image,                   -- 菜品图片
            d.description AS description        -- 菜品描述
        FROM setmeal_dish sd                    -- 套餐菜品关系表
                 JOIN dish d ON sd.dish_id = d.id        -- 菜品表
        WHERE sd.setmeal_id = #{setmealId}      -- 根据套餐ID进行筛选
    </select>
</mapper>