<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CategoryMapper">
        <update id="update" parameterType="com.sky.entity.Category">
            UPDATE category
            <set>
                <if test="name != null">name = #{name},</if>
                <if test="type != null">type = #{type},</if>
                <if test="sort != null">sort = #{sort},</if>
                <if test="status != null">status = #{status},</if>
                <if test="createTime != null">create_time = #{createTime},</if>
                <if test="updateTime != null">update_time = #{updateTime},</if>
                <if test="createUser != null">create_user = #{createUser},</if>
                <if test="updateUser != null">update_user = #{updateUser},</if>
            </set>
            WHERE id = #{id}
        </update>

    <select id="pageQuery" resultType="com.sky.entity.Category">
        SELECT *
        FROM category
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
        </where>
        <choose>
            <when test="page != null and page > 0">
                ORDER BY create_time DESC
            </when>
            <otherwise>
                ORDER BY id DESC
            </otherwise>
        </choose>
    </select>
    <select id="getList" resultType="com.sky.entity.Category">
        SELECT
        *
        FROM category      -- 假设你要查询的表是 category
        WHERE 1 = 1         -- 这个条件永远为真，作为基础条件
        <if test="type != null">  -- 判断 type 是否为空
            AND type = #{type}  -- 如果 type 不为空，则加入筛选条件
        </if>
    </select>
</mapper>